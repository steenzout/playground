---
# file: python.yml

- include: base.yml

- name: java play
  hosts: localhost
  gather_facts: yes
  connection: local

  tasks:
    - macports:
        name="{{ item }}"
        state=present
      sudo: yes
      with_items:
        - gradle
        - maven3
